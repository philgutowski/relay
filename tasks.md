# Relay self-improvement tasks

- [x] T-1 Add a shared contract for the classify digest keys. `classify.py` builds a dict that `run.py` and `closeout.py` each read as a separate string keyed lookup, and nothing asserts that the keys either module reads are the keys classify actually sets. Define the set of digest keys classify guarantees, alongside the other named contracts in `contracts.py` (near `HALT_LINES`), and add a test that fails if `run.py` or `closeout.py` reads a key not in that set, or if `classify.py` stops setting a key either module reads. This task adds a guard only; it must not change any runtime behavior. The full suite must stay green with the same test count plus the new one(s), and every existing test in `test_run.py`, `test_closeout.py`, and `test_classify.py` must still pass unchanged. (bbdeba3b528cd41d792ae2b5dde3d0b970fe50af)

- [x] T-2 The terminal record's `cli_version` field is always `contracts.CLI_VERSION_TESTED`, a pinned constant, never the version of the `claude` binary that actually ran. Read the real CLI version once per run (e.g. from `claude --version`, run through the same subprocess wrapper `launch.py` already uses, never a shell string) and record it in the terminal record alongside the pinned constant, so a version drift between the two is visible in `state.json` rather than silently invisible. Keep `CLI_VERSION_TESTED` as the pinned value Relay was tested against; add the observed value as a new field. Test with a fake `claude --version` output in the test harness, asserting both fields land in the terminal record and differ when the fake reports a different version. (c5b20a00ec39013b0a2417980930997d282252aa)

- [x] T-3 `comments_since` in the github and jira adapters returns every comment on the card when the baseline comment id is not found in the fetched list, rather than treating that as an unresolved baseline. A deleted or edited-away baseline comment currently makes `confirm_blocked_comment` read as satisfied even when the closeout wrote nothing new, because the full comment list is scanned as if the baseline were the very first comment. Change both adapters so a baseline id absent from the fetched comments is reported as unresolved rather than silently treated as "everything since the start," and add one test per adapter pinning the new behavior. Do not change the behavior when the baseline id is found or when no baseline was given. (f77468382e44552b0938ee920b15a50d11eafe77)

- [x] T-4 `CLAUDE_DIR_SCAN_REGEX` in `skills/relay/scripts/relay/contracts.py:85` matches the agent config directory prefix, written as a leading dot, then the word claude, then a slash. It matches only when the character immediately before that prefix is start of line, whitespace, a double quote, a single quote, a backtick, an open paren, or a slash. Its one production consumer is `brief.py:147`, and a hit there excludes the task from the run entirely and never launches a process, because Relay's `dontAsk` permission mode refuses an edit under that directory whatever the allowlist says. The character class omits the open square bracket and the asterisk, so a markdown link form of such a path and a bold or italic form of it, both of which tracker cards commonly contain, are not detected. Such a task launches anyway and is caught only later by the branch diff backstop, after a whole task process has been spent. Widen the character class so markdown link, bold, italic, and list marker forms match, while the prefix appearing as the suffix of a longer path segment, for example a bare word immediately before it, still does not match. Add tests pinning the newly matching forms, the forms that already matched, and the non matching suffix case; construct the literal path strings inside the test file, which is safe because the backstop at `gitwrite.py:251` filters on changed paths rather than on file contents. Then amend `docs/solutions/workflow-issues/headless-dontask-blocks-claude-dir-edits.md` to record the widened forms; do not open a new solutions doc for this. Do not change `brief.py` beyond what the wider regex produces on its own. Note that this card deliberately never writes the literal prefix, because doing so would have excluded this very task from the run. The full suite must stay green. (2467a9da595f4fe471960034d149befc9d01dc8d)

- [x] T-5 `verify.verify` takes `do_fetch=False` (`skills/relay/scripts/relay/verify.py:117`), and only the `verify` CLI verb passes `True` (`cli.py:219`). All three run loop call sites use the default (`run.py:375`, `run.py:422`, `run.py:449`), so `head_equals_remote` and `mirror_equals_head` read remote tracking refs that this runner's own push just updated. The check therefore confirms that the push command succeeded, not that the remote agrees, and cannot see a third party force push landing between the merge and the verify. Change the final full scope verify at `run.py:449` to fetch before reading, so the landing verdict is decided against the remote's actual state. Leave the two `SCOPE_CODE` verifies on the default, since they run before or instead of a push. Add a test asserting that the final verify fetches, and a test in which the remote diverges after the push and the run produces a non landed verdict rather than a landed one. Do not change `verify.startup_reverify` and do not change any halt class. The full suite must stay green. (c77c18cf25b8030a5ed66d7a0276fcf6703ac941)

- [x] T-6 A Relay run is invisible while it runs: the operator needs a second terminal and a hand written follower to see anything, which is `docs/backlog.md` line one. Add a seventh verb to the CLI, `python3 skills/relay/scripts/relay_cli.py tail <manifest>`, which follows the running task's activity and prints it decoded, one line per event, rather than the raw stream json that lands in `runner.log`. `~/.relay/manifests/watch.py` on this machine is the throwaway prototype of the decoding, reproduced here for reference: it reads a task's stdout log line by line, and for each assistant message prints the text, and for each tool use prints the tool name with the first of `command`, `file_path`, `pattern`, `skill`, or `description`. Requirements: `tail` never takes the lease, the same rule `status` follows; it works when started before the run, during it, and after it has finished; it follows across a task boundary rather than stopping at the end of one task's log; it exits 0 when the run reaches a terminal record, and 2 when the run halted, matching the existing exit code convention. Scope for this task is the CLI verb only. Do not change `/relay`'s foreground behavior and do not add macOS notifications; both are separate backlog items. Do not change `run.py`, `state.py`, or any halt class. Add tests against the stub, and document the verb in `skills/relay/SKILL.md` next to the other six and in `README.md`. The full suite must stay green. (85d3efe9b2c76aed8059fd5b207898556a64083c)

- [ ] T-7 The Closeout process runs `ce-compound` on the digest the runner composed, never the task transcript, which is deliberate under R27 (`closeout.py:150`), and its hint is only "relay task N, outcome X" (`closeout.py:164`). Its judgment is therefore only as good as the task's own envelope, and the envelope carries `status`, `blockers`, `changed_files`, and `plan_path` and nothing about what the task learned. This is `docs/backlog.md` line two, and the decision between the two options recorded there has been made: add a learnings section to the envelope. Do not give the Closeout the transcript path; that alternative was considered and rejected. Add one new optional envelope key alongside the existing four in `contracts.py`, ask for it in the task briefs at `skills/relay/templates/brief-local-merge.md` and `skills/relay/templates/brief-pr-terminal.md`, parse it into the digest wherever the other envelope keys are parsed, and render it into the Closeout brief at `skills/relay/templates/brief-closeout.md` so `ce-compound` receives it. Optional means exactly that: a task that omits the key must behave as it does today, with no new halt class, no change to `HALT_NO_ENVELOPE`, and no change to how `status` is read. Add tests for the key present, absent, and present but empty. This changes the envelope grammar, which is a contract between processes, so `CLAUDE.md` requires one live task against a throwaway target before it is done; you cannot run that from inside this run, so say so explicitly in your return envelope. The full suite must stay green.
